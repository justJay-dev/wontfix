/**
 * Seed script — placeholder.
 *
 * The old permissions catalog (permission table, role_permission table) was
 * removed as part of the migration to the Better Auth organization plugin.
 * Org-level roles (owner, admin, member) are now managed natively by the plugin
 * and require no seeding.
 *
 * This file is kept as a placeholder. Add future seed logic here as needed.
 *
 * Usage:
 *   make seed           # no-op until new seed logic is added
 */

console.log("Nothing to seed. Org roles are managed by the Better Auth organization plugin.");

